import React from "react";

function Date(props) {
  <span
    className="date"
    onClick={e => {
      this.props.displayDate(e);
    }}
  >
    {this.props.date}
  </span>;
}

export default Date;
